<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Chapter12" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Chapter12" parent="Base.Theme.Chapter12">
        <item name="actionMenuTextAppearance">@style/menu_text_style</item>
        <item name="android:actionMenuTextAppearance">@style/menu_text_style</item>
    </style>

    <style name="menu_text_style" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Menu">
        <item name="android:textSize">30sp</item>
        <item name="android:textColor">@color/red</item>
    </style>
</resources>